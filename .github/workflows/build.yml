name: Build
run-name: Running test build on ${{ github.actor }}'s deployment
on: [pull_request]
jobs:
    Run-Build:
        runs-on: ubuntu-latest
        steps:
            - name: Check out repository code
              uses: actions/checkout@v4
            - name: Setup nodejs
              uses: actions/setup-node@v4
              with:
                node-version: 22
            - name: Install dependencies
              run: npm i
            - name: 'Create env file'
              run: |
                touch .env
                echo CONTAINERID=${{ secrets.CONTAINER_ID }} >> .env
                echo DBENDPOINT=${{ secrets.DB_ENDPOINT }} >> .env
                echo DBID=${{ secrets.DB_ID }} >> .env
                echo DBKEY=${{ secrets.DB_KEY }} >> .env
                cat .env
            - name: Build site
              run: npm run build
